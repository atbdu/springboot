<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atbdu.myspringboot.dao">
    <select id="" parameterType="int" resultMap="map">
        select * from agent where 1=1
        <if test=""></if>
    </select>
</mapper>
